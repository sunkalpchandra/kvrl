name: kvrl
channels: [conda-forge, pytorch]
dependencies:
  - python=3.12
  - pytorch>=2.3
  - numpy>=1.26
  - pandas>=2.0
  - pyarrow>=15
  - scipy>=1.11
  - matplotlib>=3.8
  - pyyaml>=6
  - pip
  - pip:
      - -r requirements.txt
