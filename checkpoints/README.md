# checkpoints

Trained controller policies. Small policies (< 5 MB) may be committed as `.pt` with a
sidecar `.json` recording the training config, commit, seed and simulator metrics.
Large files are gitignored.
